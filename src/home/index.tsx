import * as React from 'react';
import Page from '../ui-common/page';
import Section from '../ui-common/section';

const Home = () => {
  return (<Page>
      <Section center bg='dark'>
        <h1>Four Key Metrics</h1>
        <p>Four areas where organizations must accelerate.</p>
      </Section>
    </Page>)
};

export default Home;
